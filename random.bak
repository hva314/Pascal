uses    crt;

var     i: longint;
        a: longint;

BEGIN

        clrscr;

        randomize;

        for i:=1 to random(1000000) do
                begin
                        a:= random(2)-1;
                        write(a);
                end;

END.
